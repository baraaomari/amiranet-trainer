# تذكيرات الإيميل — خطوة بخطوة

إيميلات مجانية من حساب Gmail، عن طريق Google Apps Script (بيتحمّل تقريباً ١٠٠ إيميل باليوم). الرسائل بتنبعت من سيرفرات جوجل نفسها، فبتوصل للبريد الوارد.

**مين بيوصله إيميل:**
- **تذكير:** الطالب الي وافق وما استعمل الموقع من ٣ أيام. بيوصله تذكير كل ٤ أيام بالكثير، وبحد أقصى ٣ تذكيرات لحد ما يرجع. التذكير بيوقف إذا امتحانه بكرا أو فات.
- **بالتوفيق:** الطالب الي امتحانه بكرا، بتوصله رسالة فيها نصايح. هاي بس للي حاطط تاريخ امتحانه بصفحة الجدول.
- **بكل إيميل:** رابط لإلغاء الاشتراك بكبسة وحدة. وبصفحة الجدول بالموقع في خيار "ذكّرني بالإيميل" بيشغّلها وبيوقفها.
- **الوقت:** كل يوم الساعة ٦ المسا بتوقيت القدس.

> ⚠️ **ممنوع تحط مفتاح `service_role` بأي مكان هون.** السكربت بيستعمل المفتاح العام (anon) مع كلمة سر خاصة بالتذكيرات. هيك لو صار إشي، ما حدا بيقدر يفتح قاعدة البيانات.

---

## ١. حساب Gmail للموقع (مستحسن)

اعمل حساب Gmail جديد خاص بالموقع، مثلاً `amiranet.trainer@gmail.com`. الطلاب رح يشوفوا هاد العنوان كمرسل، فأحسن ما يكون إيميلك الشخصي.

## ٢. قاعدة البيانات (مرة وحدة)

١. افتح Supabase ← **SQL Editor** ← **New query**.
٢. الصق كل محتوى الملف `supabase/email.sql` ← **Run**. لازم يطلع `Success`.
٣. بـquery جديد، الصق هاد وشغّله:

```sql
select value from private.settings where key = 'reminder_secret';
```

٤. انسخ الرقم الطويل الي طلع. هاد **سر التذكيرات**. لا تنشره ولا تحطه بالموقع.

## ٣. السكربت

١. بحساب الـGmail تبع الموقع، افتح https://script.google.com ← **New project**.
٢. فوق على اليسار غيّر الاسم لـ `مدرّب أميرنت — تذكيرات`.
٣. امسح كل الكود الموجود، والصق محتوى الملف `email/reminders.gs` ← 💾 **Save**.
٤. ⚙️ **Project Settings** ← انزل لـ **Script Properties** ← **Add script property**، وزيد الأربعة هدول:

| Property | Value |
|---|---|
| `SUPABASE_URL` | `https://fldilbmevccaspznbpkt.supabase.co` |
| `SUPABASE_ANON_KEY` | المفتاح العام: Supabase ← Project Settings ← API ← `anon` `public` |
| `REMINDER_SECRET` | السر من الخطوة ٢ |
| `SITE_URL` | `https://amiranet-trainer.netlify.app` |

٥. **Save script properties**.

## ٤. رابط إلغاء الاشتراك

١. فوق على اليمين: **Deploy** ← **New deployment**.
٢. جنب "Select type" اضغط ⚙️ ← **Web app**.
٣. **Execute as:** `Me` · **Who has access:** `Anyone`.
٤. **Deploy**. جوجل رح يطلب صلاحيات:
   - اضغط **Authorize access** واختار حساب الموقع.
   - إذا طلعت "Google hasn't verified this app": اضغط **Advanced** ← **Go to … (unsafe)** ← **Allow**. هاد طبيعي، لأنه السكربت إلك إنت ومش منشور لحدا.

## ٥. جرّب

فوق بالمحرّر في قائمة لاختيار الدالة. اختار الدالة، واضغط **▶ Run**:

١. **`sendTestToMe`** بيبعتلك إيميلين تجربة على نفس الحساب: تذكير، وبالتوفيق. شوف كيف مبينين بالبريد الوارد.
٢. **`dryRun`** بيوريك تحت بـ **Execution log** مين كان رح يوصله إيميل هلق، **بدون ما يبعت إشي**.

## ٦. شغّلها يومياً

اختار **`setup`** ← **▶ Run**. هاد بيعمل تشغيل تلقائي كل يوم الساعة ٦ المسا. بتشوفه بقائمة ⏰ **Triggers** على الجنب.

خلص. من هون ورايح بيشتغل لحاله.

## ٧. نزّل نسخة الموقع الجديدة

البطاقة الي بتسأل "بدك نذكّرك بالإيميل؟" موجودة بالموقع بس من النسخة الجديدة. ارفع `amirnet-site.zip` الجديد على Netlify.

---

## متابعة

- **كل تشغيل يومي:** Apps Script ← **Executions**. بكل سطر مكتوب كم واحد انبعتله، وإذا صار خطأ.
- **الأرقام:** بـSupabase SQL Editor:

```sql
-- كم واحد وافق
select count(*) filter (where reminders) as "وافقوا", count(*) as "جاوبوا" from public.email_prefs;

-- الإيميلات الي انبعتت، يوم بيوم
select sent_at::date as "اليوم", kind as "النوع", count(*) from public.email_log group by 1, 2 order by 1 desc;
```

## إذا بدك تغيّر إشي

- **الساعة:** بـ`email/reminders.gs` غيّر `atHour(18)` ← Save ← شغّل `setup` مرة ثانية.
- **نص الرسائل:** بالدالة `compose` بنفس الملف.
- **القواعد:** "٣ أيام"، "كل ٤ أيام"، "٣ تذكيرات". بتتغيّر بـ`supabase/email.sql` وبعدها تشغّله مرة ثانية. آمن للتكرار.
- **توقيف كل الإيميلات:** Apps Script ← Triggers ← احذف التشغيل.
