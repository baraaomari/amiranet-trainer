# نشر مدرّب أميرنت على موقع حقيقي بحسابات

نقل التطبيق من صفحة Claude المنشورة إلى استضافة عادية، مع تسجيل دخول حقيقي بالبريد وكلمة السر.

**الوقت المتوقع:** ٢٠–٣٠ دقيقة. **التكلفة:** مجاني، عدا المدرّس (الخطوة ٦) الذي يُحاسَب بالاستخدام.

**لن تحتاج Node.js ولا سطر أوامر.** كل شيء من المتصفح.

---

## قبل أن تبدأ

سأبني الملفات، لكن **إنشاء الحسابات مهمتك أنت** — لا أنشئ حسابات ولا أدخل كلمات سر نيابةً عن أحد. ستحتاج حسابين:

| الخدمة | لماذا | التكلفة |
|---|---|---|
| [Supabase](https://supabase.com) | الحسابات وقاعدة البيانات | مجاني — ٥٠ ألف مستخدم شهرياً، ٥٠٠ ميغا |
| [Netlify](https://app.netlify.com/drop) | استضافة الصفحة | مجاني — وسحب وإفلات بدون حساب |

---

## ١. أنشئ مشروع Supabase

1. ادخل [supabase.com](https://supabase.com) و**New project**
2. اختر اسماً، وكلمة سر لقاعدة البيانات (احفظها، لن تحتاجها هنا لكن لا تضيّعها)، وأقرب منطقة — **Frankfurt** أو **London** أقرب لك
3. انتظر دقيقتين حتى يجهز

## ٢. أنشئ الجداول

1. من القائمة الجانبية: **SQL Editor** → **New query**
2. افتح `supabase/schema.sql` من مجلد المشروع، انسخ محتواه كاملاً، والصقه
3. **Run**

هذا ينشئ جدولين ويفعّل **Row Level Security**: كل مستخدم يرى بياناته وحده، ولا يستطيع أحد — ولا أنت — قراءة بيانات غيره من التطبيق.

## ٣. خذ مفاتيح المشروع

**Project Settings** → **API**، وانسخ:

- **Project URL** — مثل `https://abcdefgh.supabase.co`
- **anon / public key** — نص طويل يبدأ بـ`eyJ…`

> مفتاح `anon` **مصمَّم ليكون علنياً** في الصفحة؛ الحماية من سياسات RLS في الخطوة ٢.
> **لا تأخذ `service_role` أبداً** — هذا يتجاوز كل الحماية.

## ٤. املأ ملف الإعداد

افتح `web/config.js` واستبدل القيمتين:

```js
window.SUPABASE_CONFIG = {
  url: "https://abcdefgh.supabase.co",
  anonKey: "eyJ...",
  tutorFunction: "tutor"
};
```

إذا لن تنشر المدرّس الآن، اجعل `tutorFunction: ""` وسيختفي المدرّس من الواجهة بدل أن يظهر معطّلاً.

## ٥. انشر الموقع

```bash
python build.py && python build_web.py
```

ثم اسحب **مجلد `web` كاملاً** إلى [app.netlify.com/drop](https://app.netlify.com/drop).

خلال ثوانٍ يعطيك رابطاً مثل `https://something-random.netlify.app`. من **Site settings → Change site name** غيّره لاسم تحبه.

> ارفع المجلد لا الملفات المفردة — `index.html` و`config.js` يجب أن يكونا معاً.

**عُد إلى Supabase → Authentication → URL Configuration** وضع رابط موقعك في **Site URL**. بدونها روابط تأكيد البريد لن تعمل.

---

## ٦. المدرّس (اختياري — هنا التكلفة)

المدرّس داخل صفحة Claude كان يعمل على حسابك بلا تكلفة إضافية. خارجها يحتاج مفتاح Anthropic API، **والاستخدام يُحاسَب عليك**.

> **مهم:** لا يوضع مفتاح API في `config.js` ولا في أي ملف يصل المتصفح. الصفحة الثابتة يقرأها أي زائر، فالمفتاح فيها مكشوف ويمكن لأي أحد استنزافه على حسابك. لذلك يبقى المفتاح في دالة على الخادم.

1. احصل على مفتاح من [console.anthropic.com](https://console.anthropic.com) → API Keys
2. في Supabase: **Edge Functions** → **Secrets** → أضف
   `ANTHROPIC_API_KEY` = مفتاحك
3. **Edge Functions** → **Deploy a new function** → **Via Editor**
4. سمّها **`tutor`** بالضبط (نفس ما في `config.js`)
5. الصق محتوى `supabase/tutor.ts` كاملاً → **Deploy**

الدالة تتحقق من تسجيل الدخول، وتحدّ حجم الطلب، ولا تكشف المفتاح.

**للتحقق:** افتح موقعك، سجّل دخولك، اعمل امتحاناً، واضغط «ليش هاد الجواب؟». إن ظهر خطأ فافتح **Edge Functions → tutor → Logs**.

---

## التحقق النهائي

| الاختبار | المتوقع |
|---|---|
| افتح الموقع | تظهر شاشة تسجيل الدخول |
| «حساب جديد» ببريدك | رسالة تأكيد تصل بريدك |
| أكّد ثم سجّل دخولك | يفتح التطبيق |
| اعمل جلسة حفظ ثم أعد التحميل | التقدّم يعود |
| افتح الموقع من جهاز آخر وسجّل الدخول | **نفس التقدّم** — هذا هو الفرق الحقيقي |
| زر الخروج في الرأس | يعود لشاشة الدخول |

إن ظهر تنبيه أصفر «التقدّم محفوظ على هذا المتصفح فقط» فمعناه أن `config.js` لم يُقرأ أو أن القيم خاطئة — افحص الـConsole في المتصفح.

---

## التحديث لاحقاً

```bash
python build.py && python build_web.py
```

ثم اسحب `web` مرة أخرى إلى نفس الموقع في Netlify (**Deploys** → منطقة السحب). `config.js` لا يُستبدَل، فقيمك تبقى.

---

## نقاط تستحق الانتباه

**ملف واحد يعمل في المكانين.** التطبيق يكتشف بيئته عند التشغيل: داخل صفحة Claude يستعمل قدراتها، وعلى الاستضافة يستعمل Supabase، وبدون الاثنين يحفظ محلياً. لا نسختين تتباعدان.

**التقدّم القديم لا ينتقل.** ما حُفظ في صفحة Claude يبقى هناك؛ الحساب الجديد يبدأ من الصفر.

**تأكيد البريد مفعّل افتراضياً.** لتعطيله أثناء التجربة: **Authentication → Providers → Email** → أطفئ *Confirm email*. أعده قبل أن تفتح الموقع لغيرك.

**حقوق المحتوى لم تتغيّر.** الامتحانات الخمسة مكتوبة خصيصاً لهذا التطبيق ولا حقوق طرف ثالث عليها، لكن **الـ٥٨٠ كلمة من كتاب TBT ما زالت محميّة**. على استضافة عامة يصبح الموقع مفتوحاً لأي شخص يملك الرابط — وهذا نشر، لا استعمال شخصي. إن أردت فتح الموقع للناس، احذف كلمات الكتاب وأبقِ الـ١١٣ التي كُتبت خصيصاً (`data/vocab_extra.json`).
