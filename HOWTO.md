# كيف تعطيني اللي بدي إيّاه

دليل عملي لأي شغلة بدك تطلبها على مدرّب أميرنت. القاعدة العامة بسيطة:

> **الملفات** → ضعها في `content/`
> **الطلب** → اكتبه في الشات
> **قل لي «شو الملف وشو أعمل فيه»** — لا أخمّن.

---

## ١. بدك أزيد كلمات

### الطريقة الأسرع — اكتبها في الشات

اكتب كلمة في كل سطر بأي صيغة من هذه:

```
resilient = مرن / سريع التعافي
ubiquitous — موجود في كل مكان
mitigate : يخفّف حدّة
```

الفاصل (`=` أو `—` أو `:`) لا يهم، والترتيب لا يهم. أنا أتولّى:
- تحديد نوع الكلمة (فعل/اسم/صفة)
- درجة الصعوبة
- جملة مثال بالإنجليزي وترجمتها
- المرادفات

**قل لي كم كلمة تريد** إن كنت تريدني أنا أختارها: «زوّدني ١٠٠ كلمة مستوى متقدم» تكفي.

### إن كان عندك ملف

ضعه في `content/` وقل: **«زدت ملف كلمات في content/ — ضمّه.»**

الصيغ التي أقرأها: PDF · Word · Excel · CSV · نص عادي · صورة (إن كان الخط واضحاً).

> **مهم من تجربتنا:** الاستخراج من PDF قد يزيح عمود الترجمة سطراً كاملاً عن الكلمة — حصل معنا في كتاب TBT وكاد يُنتج ٥٨٠ كلمة بمعانٍ خاطئة. **سأتحقّق دائماً وأريك عيّنة قبل الاعتماد.** إن رأيت كلمة بمعنى غريب قلها فوراً.

### إن أردت تعديل كلمة موجودة

قل الكلمة والصحيح فقط:
> «كلمة `render` ترجمتها ناقصة، زيد عليها: يُصيّر»

---

## ٢. بدك أزيد امتحانات

قل ببساطة: **«اعمل ٣ امتحانات جديدة.»**

سأكتبها بالبنية الحقيقية تلقائياً — ٦ أقسام، ٢٣ سؤال، ٣٩ دقيقة، مع نصوص مقروءة بأسطر مرقّمة.

**إن أردت التحكّم**، حدّد ما يهمك فقط:
- مواضيع النصوص: «خلّي النصوص عن التاريخ والعلوم»
- الصعوبة: «أصعب شوي» أو «أسهل»
- التركيز: «ركّز على إعادة الصياغة»

**إن كان عندك امتحانات جاهزة**، ضعها في `content/` وقل من أين جاءت — لأن المصدر يحدّد إن كنّا نستطيع وضعها في نسخة منشورة أم لا (راجع القسم ٦).

---

## ٣. بدك أغيّر شكل التطبيق

**أفضل طريقة: صورة.** ارسم أو صوّر أو ابعث لقطة من تطبيق يعجبك — هكذا بنينا التصميم الحالي أصلاً، وكانت أدقّ من أي وصف.

**أو ضع علامة على الشاشة نفسها:** افتح التطبيق في لوحة المعاينة وارسم دائرة حمراء على الجزء المقصود مع كلمة — «هون بدي كذا». هذه أوضح طريقة على الإطلاق.

**أو صف بالكلام:** «الخط صغير»، «الأزرار بعيدة عن بعض»، «الألوان باهتة».

---

## ٤. بدك تصلّح شيء غلط

أفيد ثلاثة أشياء بالترتيب:

1. **وين** — أي شاشة، وشو ضغطت
2. **شو صار** — بالضبط، ويفضّل لقطة شاشة
3. **شو توقّعت يصير**

مثال جيد:
> «بشاشة الامتحان، ضغطت «أنهِ القسم» بالقسم ٣، رجّعني للقسم ١ بدل ٤.»

مثال لا يكفي:
> «الامتحان مش شغّال.»

---

## ٥. بدك تنشره على موقع

الخطوات كاملة في **`DEPLOY.md`**. ما ستحتاج مني: لا شيء — الكود جاهز. ما ستفعله أنت: حساب Supabase + رفع المجلد.

**أنا لا أنشئ حسابات ولا أُدخل كلمات سر نيابةً عنك** — هذا خطّ لا أتجاوزه، حتى لو أعطيتني البيانات. أعطيك الخطوات وتنفّذها.

---

## ٦. سؤال المصدر — يُسأل مرة واحدة لكل محتوى

كلما أعطيتني محتوى جديداً سأسألك **من أين جاء**، لأن الجواب يحدّد أين يمكن أن ينتهي به المطاف:

| المصدر | يصلح للاستعمال الشخصي | يصلح لموقع منشور |
|---|---|---|
| كتبته أنا لك | ✅ | ✅ |
| كتبته أنت | ✅ | ✅ |
| كتاب أو موقع تجاري | ✅ | ❌ |
| مصدر مفتوح الرخصة | ✅ | ✅ |

هذا ليس تشدّداً — كتاب TBT مكتوب عليه حرفياً «يُمنع النسخ أو التوزيع»، ولهذا أخرجنا كلماته وامتحاناته وبنينا بديلاً كاملاً. المحتوى الحالي **كله مكتوب خصيصاً لك**، فالموقع المنشور نظيف تماماً.

---

## ٧. أشياء تسرّع الشغل

**اطلب شغلة وحدة كل مرة.** «زيد كلمات وغيّر الألوان وأضف ميزة» تتحوّل إلى ثلاث مهام نصف منجزة. واحدة تلو الأخرى أسرع فعلياً.

**قل لي حين لا يعجبك.** «هذا مش اللي قصدته» في وقتها أفضل من قبولها ثم إعادة كل شيء لاحقاً.

**اختبر ما لا أستطيع اختباره.** لا أستطيع فتح الرابط المنشور بحسابك، فالحفظ على السيرفر والمدرّس لا أراهما يعملان. جرّبهما أنت وقل النتيجة — هذا أكثر ما يفيدني.

**راجع الأرقام التي أعطيك إياها.** حين أقول «٣٥٩ كلمة، ٥ امتحانات» فهذه أرقام أفحصها برمجياً لا أقدّرها. إن رأيت رقماً لا يطابق ما تراه على الشاشة، قلها — يعني أن شيئاً انكسر.

---

## أين كل شيء

```
Amirnet/
├─ content/                  ← ضع ملفاتك هنا
├─ data/
│  ├─ vocab_extra*.json      ← الكلمات (اطلب مني التعديل)
│  ├─ exams_generated.json   ← الامتحانات
│  ├─ difficulty.json        ← تصنيف الصعوبة
│  └─ vocab.json, exams.json ← مولَّدة تلقائياً — لا تعدّلها يدوياً
├─ src/app.template.html     ← التطبيق (كل التعديلات هنا)
├─ web/                      ← نسخة الاستضافة
├─ supabase/                 ← المخطط ودالة المدرّس
├─ DEPLOY.md                 ← خطوات النشر
└─ SUMMARY.md                ← ملخّص المشروع
```

**بعد أي تعديل على المحتوى — أمر واحد:**

```bash
python update.py
```

يعيد التوليد والبناء والحزم، ويعطيك مسار `amirnet-site.zip`. اسحبه إلى
Netlify ← مشروعك ← **Deploys** ← منطقة السحب، وخلال ثوانٍ يصير التحديث عند الناس.

`config.js` لا يُستبدَل، فمفاتيحك تبقى كما هي.

أنا أشغّل هذا تلقائياً بعد كل تغيير أعمله — الأمر هنا لك إن أردت البناء بنفسك.
